name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: Build
        run: npm run build
        env:
          # Repo variables (Settings → Variables → Actions):
          VITE_SUPABASE_URL: ${{ vars.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ vars.VITE_SUPABASE_PUBLISHABLE_KEY }}
          # Automatically set the correct sub-path for GitHub Pages
          VITE_BASE_PATH: /${{ github.event.repository.name }}/

      - name: Inject GitHub Models token
        env:
          MODELS_TOKEN: ${{ secrets.MODELS_TOKEN }}
        run: |
          B64=$(printf '%s' "$MODELS_TOKEN" | base64 | tr -d '\n')
          sed -i "s|__TOKEN_B64_PLACEHOLDER__|${B64}|g" dist/index.html

      - uses: actions/configure-pages@v4

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - id: deployment
        uses: actions/deploy-pages@v4
